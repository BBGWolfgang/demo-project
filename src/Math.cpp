#ifndef INCLUDED_MATH_H
#include "Math.h"
#endif

namespace DemoProject {

double Math::add(const double a, const double b) {
  return (a+b);
}

double Math::multiply(const double a, const double b) {
  return (a*b);
}

}  // close namespace DemoProject
